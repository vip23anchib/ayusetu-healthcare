def queue_booking_notifications(appointment_id):
    print(f"[Stub] Queue booking confirmation notifications for appointment {appointment_id}")

def queue_cancellation_notifications(appointment_id):
    print(f"[Stub] Queue cancellation notifications for appointment {appointment_id}")

def queue_reschedule_notifications(appointment_id):
    print(f"[Stub] Queue reschedule notifications for appointment {appointment_id}")

def queue_leave_notifications(appointment_id):
    print(f"[Stub] Queue doctor leave cancel notifications for appointment {appointment_id}")
